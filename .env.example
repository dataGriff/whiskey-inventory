# Database Configuration for Local Development and Docker
# Copy this file to .env and update with your settings

# Database Configuration
POSTGRES_USER=whiskey_user
POSTGRES_PASSWORD=whiskey_dev_password
POSTGRES_DB=whiskey_inventory
POSTGRES_PORT=5432

# Application Ports
BACKEND_PORT=3001
FRONTEND_PORT=3000

# PostgreSQL Connection URL
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=SCHEMA
# 
# For local development with docker-compose:
DATABASE_URL="postgresql://whiskey_user:whiskey_dev_password@localhost:5432/whiskey_inventory?schema=public"

# For Docker Compose (internal network):
# DATABASE_URL="postgresql://whiskey_user:whiskey_dev_password@postgres:5432/whiskey_inventory?schema=public"

# Node Environment
NODE_ENV=production

# Note: Never commit the .env file with real credentials to version control
